package com.example.make_a_friend; // Change this package name to match your project package

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class MoreIconActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.more_icon_dashb); // Set the layout for this activity
    }
}
